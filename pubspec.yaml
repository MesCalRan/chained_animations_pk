name: chained_animations_pk
description: Chained Animations Package
version: 0.0.1
homepage: https://github.com/MesCalRan/chained_animations_pk

environment:
  sdk: '>=2.18.5 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter: